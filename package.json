{
  "name": "ends-and-means",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=26 <27"
  },
  "scripts": {
    "dev": "node scripts/environment-entry.mjs command && astro dev",
    "build": "node scripts/environment-entry.mjs command && pnpm validate && node scripts/build-environment.mjs",
    "preview": "node scripts/environment-entry.mjs focused && astro preview",
    "lint": "node scripts/environment-entry.mjs command && biome lint --error-on-warnings . && pnpm lint:markdown",
    "lint:fix": "node scripts/environment-entry.mjs command && biome lint --write . && pnpm lint:markdown --fix",
    "lint:markdown": "node scripts/environment-entry.mjs command && markdownlint-cli2 && pnpm lint:narrative",
    "lint:narrative": "node scripts/environment-entry.mjs command && node --import tsx scripts/lint-narratives.ts",
    "static": "node scripts/environment-entry.mjs command && knip --include files,dependencies,unlisted,binaries,unresolved,cycles",
    "check": "node scripts/environment-entry.mjs command && astro check",
    "test": "node scripts/environment-entry.mjs command && vitest run tests/content tests/delivery tests/skills",
    "test:coverage": "node scripts/environment-entry.mjs command && vitest run tests/content tests/delivery tests/skills --coverage",
    "test:routes": "node scripts/environment-entry.mjs command && vitest run tests/routes",
    "test:visual": "node scripts/environment-entry.mjs focused && playwright test",
    "review:visual": "node scripts/environment-entry.mjs focused && playwright test",
    "validate": "node scripts/environment-entry.mjs command && node --import tsx scripts/validate-domain.ts",
    "audit:content": "node scripts/environment-entry.mjs command && node --import tsx scripts/audit-content.ts",
    "audit:content-preflight": "node scripts/environment-entry.mjs focused && node --import tsx scripts/content-semantic-preflight.ts",
    "audit:corpus-diversity": "node scripts/environment-entry.mjs command && node --import tsx scripts/audit-corpus-diversity.ts",
    "audit:provenance": "node scripts/environment-entry.mjs command && node --import tsx scripts/audit-provenance.ts",
    "inventory:dependencies": "node scripts/environment-entry.mjs command && node --import tsx scripts/generate-lockfile-provenance.ts",
    "audit:delivery": "node scripts/environment-entry.mjs command && node --import tsx scripts/audit-repository-delivery.ts && node --import tsx scripts/audit-skill-contracts.ts && node --import tsx scripts/audit-delivery.ts",
    "audit:content-integrity": "node scripts/environment-entry.mjs command && node --import tsx scripts/verify-content-integrity.ts",
    "verify": "node scripts/environment-entry.mjs verify && node scripts/verify-environment.ts",
    "verify:checks": "node scripts/environment-entry.mjs verify && pnpm audit:delivery -- --repository-only && pnpm audit:provenance && pnpm audit:corpus-diversity && pnpm audit:content-preflight && pnpm lint && pnpm static && pnpm audit --audit-level=moderate && pnpm check && pnpm test:coverage && pnpm build && pnpm audit:content-integrity && pnpm test:routes && pnpm test:visual",
    "environment:check": "node scripts/check-environment.mjs"
  },
  "dependencies": {
    "@lucide/astro": "^1.41.0",
    "astro": "^7.3.1",
    "micromark": "^4.0.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@astrojs/compiler": "^4.0.0",
    "@biomejs/biome": "^2.5.12",
    "@emnapi/core": "^1.11.3",
    "@emnapi/runtime": "^1.11.3",
    "@playwright/test": "^1.63.0",
    "@types/css-tree": "^3.2.0",
    "@types/node": "^26.4.1",
    "@vitest/coverage-v8": "^5.0.0",
    "css-tree": "^3.2.1",
    "knip": "^6.34.0",
    "markdownlint-cli2": "^0.23.2",
    "markdownlint-sentences-per-line": "^0.1.4",
    "postcss": "^8.5.28",
    "tsx": "^4.23.13",
    "typescript": "^6.0.3",
    "vitest": "^5.0.0",
    "yaml": "^2.9.0",
    "zod": "^4.5.4"
  },
  "knip": {
    "ignoreDependencies": [
      "@emnapi/core",
      "@emnapi/runtime"
    ]
  }
}
