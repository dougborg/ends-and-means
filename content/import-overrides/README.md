# Import overrides

`overrides.json` is the human-reviewed input to the source-document importer. The
importer deliberately does not turn prose verdicts, parenthetical references, or
ambiguous reading-list bullets into graph relationships on its own.

Keys in this file are stable imported IDs. Add mappings only after editorial
review; unknown IDs and malformed mappings cause the import to fail.

