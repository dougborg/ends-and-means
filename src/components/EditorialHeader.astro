---
interface Props {
  coordinate: string;
  title: string;
}

const { coordinate, title } = Astro.props;
---

<header class="editorial-header">
  <p class="coordinate">{coordinate}</p>
  <h1>{title}</h1>
  <slot />
</header>
