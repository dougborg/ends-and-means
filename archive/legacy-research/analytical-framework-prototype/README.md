# Archived analytical prototype

This directory preserves an early non-canonical prototype. Its former
validator, tests, and `/prototype/` route have been removed. The
fixture is not executable and nothing here is a production input.

Use `prototype.json` and `migration-notes.md` only as historical design context
when evaluating candidate records against the current model.
