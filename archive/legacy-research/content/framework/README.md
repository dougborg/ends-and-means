# Archived framework migration draft

`draft.json` preserves the complete intermediate migration of the exploratory
comparison matrix. It is no longer generated, validated, or rendered. The
original per-record disposition audit is preserved at
`../../reports/framework-migration.json`.

The file is a discovery aid for backlog research, not a source of publishable
claims. Any useful fragment must undergo fresh source review, reconciliation,
and pull-request approval before entering `content/domain/`.
