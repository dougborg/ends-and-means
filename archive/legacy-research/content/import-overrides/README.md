# Archived import overrides

`overrides.json` records human reconciliation choices used by the retired
source-document importer. The importer and its commands no longer exist.

These mappings are retained solely to explain historical migration decisions;
they are not stable canonical IDs or active configuration.
