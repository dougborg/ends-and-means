# End change

An End is something identifiable actors or texts say political or economic
arrangements ought to achieve. It is never an unattributed universal value.

Include `description`, `attributions` (each with `holder`, `claimIds`, and
`context`), `tensions`, and `scope`. Separate an actor's stated End from an
editor's inference and from observed outcomes.
