# Means proposal

A Means is an institutional mechanism or arrangement: who may act, through what
rules, with which resources, oversight, and enforcement.

Include `description`, `institutionalForm`, `actors`, `decisionRules`,
`enforcement`, `conditions`, and `failureModes`. Distinguish formal design from
implementation and observed operation. Add only source-supported relationships.
